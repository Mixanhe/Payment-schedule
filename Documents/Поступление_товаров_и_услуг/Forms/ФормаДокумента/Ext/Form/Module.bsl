
&НаКлиенте
Процедура НайтиСвязаныеДокументы(Команда)
	
	Макет = ПолучитьМакетНаСервере();
	
	Если Не Макет = Неопределено Тогда
		Макет.Показать("Связанные Документы");
	Иначе
		Сообщить("Нет документов!");
	КонецЕсли;
	
КонецПроцедуры

Функция ПолучитьМакетНаСервере()
	
	Возврат Пробник.ПолучитьСвязанныеДокументыНаСервере(Объект.Ссылка);	
	
КонецФункции
